---
kind: pipeline
type: docker
name: cluster-hk-01

steps:
  - name: build
    image: harbor.jinhun.moe/library/mdbook_builder:0.4.15
    commands:
      - mdbook build
  - name: publish
    image: plugins/docker:latest
    settings:
      dockerfile: .ci/Dockerfile
      tag:
        - latest
        - ${DRONE_BUILD_NUMBER}
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      registry: https://harbor.jinhun.moe
      repo: harbor.jinhun.moe/library/doc_share
  - name: deploy
    image: harbor.jinhun.moe/drone/deploy:latest
    settings:
      host:
        from_secret: deploy_host
      username: 
        from_secret: deploy_username
      key:
        from_secret: deploy_key
      script:
        from_secret: deploy_script
---
kind: signature
hmac: d2bdd316dc96d6b1d8dd85b2e2eccbb0c13308e0e9534dba0c663ea5b9a6846a

...
